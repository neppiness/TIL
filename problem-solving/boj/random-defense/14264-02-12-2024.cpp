#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  cout << fixed;
  cout.precision(15);
  double l; cin >> l;
  double ans = sqrt(3) / 4.0 * l * l;
  cout << ans;
}
