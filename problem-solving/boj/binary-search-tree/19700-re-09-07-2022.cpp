#include <bits/stdc++.h>
using namespace std;

int RANK[500002];

int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);

  int n, h, r;
  cin >> n;
  for(int i = 1; i <= n; i++){
    cin >> h >> r;
    RANK[h] = r;
  }

  int cnt = 0;
  do {

  } while(--n);
}