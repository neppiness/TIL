#include <bits/stdc++.h>
using namespace std;

int t;

int solve() {
  int left, right, x;
  cin >> left >> right >> x;

  int a, b;
  cin >> a >> b;
  if(a == b) return 1;

  
}

int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);

  cin >> t;
  while(t--)
    cout << solve() << '\n';
}